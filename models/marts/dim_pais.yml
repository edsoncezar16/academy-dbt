version: 2
 
models:
 - name: dim_pais
   columns:
     - name: sk_pais
       description: A chave surrogate do pais.
       tests:
         - unique
         - not_null
    
     - name: codigo_pais
       description: A chave natural do pais.

     - name: pais
       description: Nome do pais.